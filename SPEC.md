# family-dokoiku 仕様

## 1. 目的

`family-dokoiku` は、吉祥寺〜新宿周辺を中心に、家族で「実際に行きたくなる」おでかけ先を少数精鋭で掲載する。

網羅性は目指さない。検索結果を大量に並べるのではなく、次の判断を短時間でできることを優先する。

- 何ができるか
- いつ行けるか
- どこにあるか
- いくらかかるか
- 予約が必要か
- 現地でざっくり何時間過ごせるか

常設施設、見るだけのイベント、似たイベントの大量掲載は避ける。

---

## 2. カテゴリ

### 2.1 `kodomoto` — こどもと

子どもだけでなく、保護者も一緒に楽しめる単発・期間限定のイベント。

主な対象例:

- 工作、科学、料理、職業体験
- ゲーム、ボードゲーム、スポーツなどの参加型イベント
- 屋台や縁日が多く、会場全体を楽しめるイベント
- 珍しい室内体験
- 普段できない場所・設備を使う体験
- 季節限定・期間限定の参加型イベント
- 一つの会場で複数の体験を楽しめるイベント

#### 掲載しないもの

- 展示を見るだけ
- ステージやショーを見るだけ
- 物販だけ
- パネル展示だけ
- 小規模な読み聞かせなど、わざわざ出かける理由が弱いもの
- 常設プログラム
- 動物園・水族館
- 動物ふれあい、移動動物園などを含む動物系イベント

イベント数が多い場合は、珍しさ、体験の多さ、保護者側の楽しさ、現地で過ごせる時間を優先して絞る。

### 2.2 `family_event` — 家族イベント

誕生日、記念日、家族のお祝いなど「少し特別なおでかけ」に使うイベント・プラン。

基本はホテルのランチビュッフェとする。

#### 掲載条件

- 大人1人の基準料金が、おおむね 3,000〜7,000円
- 家族で昼に利用しやすい
- 誕生日・記念日などに使いやすい
- 肉料理、洋食、デザートなど家族で選びやすい内容を優先
- 通常営業そのものより、季節フェア・期間限定・特別プランを優先

#### 掲載しないもの

- 魚介・シーフードが主役のプラン
- 酒を楽しむことが前提のプラン
- 居酒屋業態
- 常設の通常プランだけで、期間限定性や特別感が弱いもの

---

## 3. エリア

### 3.1 基本エリア

通常の掲載対象は **吉祥寺〜新宿周辺**。

厳密な行政区境界ではなく、家族で吉祥寺〜新宿エリアから出かけやすい範囲を編集上の基準とする。

### 3.2 キャラクターイベントの例外

キャラクターイベントは **東京都内** まで対象を広げる。

対象コンテンツは次の3つだけ。

- サンリオ
- ポケモン
- パウ・パトロール

さらに、次の条件をすべて満たすこと。

- 公式サイト、公式SNS、公式施設、公式主催者などの **公式発信で確認できる**
- 期間限定または単発
- 体験、ゲーム、ワークショップ、スタンプラリーなど参加要素がある

物販だけ、パネル展示だけ、見るだけのショーは掲載しない。

上記3コンテンツ以外のキャラクターイベントは原則掲載しない。

---

## 4. 常設イベントの扱い

**常設は原則掲載しない。**

掲載対象は、単発・期間限定・季節限定・特別開催を基本とする。

施設そのものが常設でも、その期間だけ実施される特別体験や期間限定企画は対象にできる。ただし動物園・水族館・動物系イベントは除外する。

---

## 5. 料金の定義

`price` はカテゴリによって基準が異なる。

- `kodomoto`: **子ども1人分の料金**
- `family_event`: **大人1人分の料金**

無料は `0`。

料金体系が複雑な場合は、代表的な料金を `price` に入れ、補足を `priceNote` に記載する。

---

## 6. 滞在時間の定義

`stayHours` は **現地に到着してから、その場所でざっくり何時間過ごせるか** の目安。

移動時間は一切含めない。

例:

- 45〜90分程度なら `1`
- 2時間前後なら `2`
- 2〜3時間程度なら `2.5`
- 4時間程度楽しめるなら `4`

「半日」「一日」などの曖昧な区分は使わず、時間数で持つ。

---

## 7. 地図表示

各イベントには住所と緯度・経度を持たせ、サイト上でマップにピン表示できるようにする。

必要項目:

- `address`
- `lat`
- `lng`

一覧と地図は同じ `events.json` を参照する。

フィルタ後のイベントだけを地図に表示できる構成を想定する。

---

## 8. 予約

`reservation` は次のいずれか。

- `required`: 予約必須
- `recommended`: 予約推奨、または定員制で事前申込が有利
- `not_required`: 予約不要
- `unknown`: 取得元で判断できない

---

## 9. 日付

- `startDate`: 開始日、`YYYY-MM-DD`
- `endDate`: 複数日開催の場合のみ設定。単日イベントでは省略可能
- 日ごとに内容や会場が異なる場合は別イベントとして登録する

終了済みイベントは通常の閲覧画面では表示しない。

---

## 10. 情報源

すべてのイベントに `sourceUrl` と `retrievedDate` を持たせる。

情報源は、主催者、会場、自治体、公式施設など、開催内容を確認できる一次情報を優先する。

キャラクターイベントは `officialSource: true` が必須。

---

## 11. JSON項目

`events.json` はイベントオブジェクトの配列。

### 必須

| 項目 | 型 | 内容 |
| --- | --- | --- |
| `id` | string | 安定した一意ID。英小文字・数字・ハイフン推奨 |
| `category` | string | `kodomoto` / `family_event` |
| `title` | string | イベント名 |
| `startDate` | string | 開始日 |
| `venueName` | string | 会場名 |
| `address` | string | 住所 |
| `lat` | number | 緯度 |
| `lng` | number | 経度 |
| `price` | number/null | 基準料金。無料は0、不明はnull |
| `stayHours` | number | 現地で過ごせる時間の目安 |
| `reservation` | string | 予約要否 |
| `tags` | string[] | 体験内容・特徴 |
| `sourceUrl` | string | 取得元URL |
| `retrievedDate` | string | 情報確認日 |
| `officialSource` | boolean | 公式発信か |

### 任意

| 項目 | 型 | 内容 |
| --- | --- | --- |
| `endDate` | string | 終了日 |
| `targetAge` | string | 対象年齢の表示文 |
| `priceNote` | string | 料金補足 |
| `summary` | string | 何ができるかの短い説明 |
| `character` | string | `sanrio` / `pokemon` / `paw_patrol` |
| `notes` | string | その他の編集メモ |

---

## 12. JSON例

```json
[
  {
    "id": "2026-10-12-kichijoji-science",
    "category": "kodomoto",
    "title": "親子サイエンス体験",
    "startDate": "2026-10-12",
    "venueName": "○○ホール",
    "address": "東京都武蔵野市...",
    "lat": 35.0,
    "lng": 139.0,
    "price": 500,
    "stayHours": 2,
    "reservation": "not_required",
    "targetAge": "小学生",
    "tags": ["science", "hands_on", "indoor"],
    "summary": "複数の実験ブースを親子で体験できる",
    "sourceUrl": "https://example.com/event",
    "retrievedDate": "2026-09-20",
    "officialSource": true
  }
]
```

---

## 13. 更新運用

イベントの調査・選定・追加・修正・削除は、ChatGPTとの会話からGitHub上の `events.json` を更新する運用を基本とする。

専用CMSや有料AI APIを前提にしない。

更新時は最低限、次を確認する。

1. 現在の掲載ルールに合うか
2. 既存イベントと重複しないか
3. 開催日・会場・料金・予約要否を取得元で確認できるか
4. 住所と座標が会場と一致しているか
5. `stayHours` が移動時間を含んでいないか
6. キャラクターイベントなら東京都内かつ公式発信か
7. 終了済みイベントを新規追加していないか

このサイトは「全部載っていること」より、「開いたときに行きたくなる候補だけが残っていること」を優先する。
